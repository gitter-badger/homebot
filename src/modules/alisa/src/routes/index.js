import express from 'express';

//import devices from '../controllers/???';

const routes = express.Router({ mergeParams: true })

// routes.route('/')
//   .get(???.list)
//   .post(???.create)

module.exports = routes;